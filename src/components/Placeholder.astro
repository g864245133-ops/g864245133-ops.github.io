---
interface Props {
  title: string;
  text: string;
}

const { title, text } = Astro.props;
---

<div class="placeholder">
  <p class="placeholder-label">To be added</p>
  <h2>{title}</h2>
  <p>{text}</p>
</div>
